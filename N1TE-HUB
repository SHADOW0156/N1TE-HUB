--[[🌌 N1TE HUB -V1--]]
repeat task.wait() until game:IsLoaded()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()


local DEEP_BLUE = Color3.fromRGB(0, 40, 120)
local BORDER_BLUE = Color3.fromRGB(0, 80, 255)
local BG_BLACK = Color3.fromRGB(10, 10, 10)
local BUTTON_BLACK = Color3.fromRGB(20, 20, 20)
local TEXT_WHITE = Color3.fromRGB(255, 255, 255)


local Settings = {
    Aimbot = { Enabled = false, TeamCheck = true, WallCheck = true, Smoothness = 0.1, AimPart = "Head", FOV = 150, Visible = true, Color = BORDER_BLUE },
    Visuals = { Enabled = true, Boxes = true, Names = true, Tracers = true, Health = true, Distance = true, Chams = false, Rainbow = false, Skeleton = false, HeadDot = false },
    Movement = { WalkSpeed = 16, JumpPower = 50, InfiniteJump = false, NoClip = false, Fly = false, FlySpeed = 50, Gravity = 196.2, SpinBot = false, AntiRagdoll = true, Spider = false, WaterWalk = false },
    Utility = { AntiAFK = true, NoFog = false, FullBright = false, FOV = 70, ClickTP = false, ChatSpam = false },
    Combat = { HitboxEnabled = false, HitboxSize = 2, HitboxTransparency = 0.7, TriggerBot = false }
}

local ESPCache = {}
local FlyBodyVelocity = nil
local FlyBodyGyro = nil


local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.NumSides = 100
FOVCircle.Filled = false
FOVCircle.Transparency = 1
FOVCircle.Visible = false
FOVCircle.Radius = Settings.Aimbot.FOV
FOVCircle.Color = BORDER_BLUE


local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "N1TE_HUB_V2"
ScreenGui.Parent = CoreGui
ScreenGui.DisplayOrder = 9999

local OpenButton = Instance.new("ImageButton")
OpenButton.Name = "OpenButton"
OpenButton.Parent = ScreenGui
OpenButton.Size = UDim2.new(0, 75, 0, 75)
OpenButton.Position = UDim2.new(0, 20, 0.5, -37.5)
OpenButton.BackgroundColor3 = BG_BLACK
OpenButton.Image = "rbxassetid://6031094678"
OpenButton.ImageColor3 = BORDER_BLUE
local oCorner = Instance.new("UICorner")
oCorner.CornerRadius = UDim.new(1, 0)
oCorner.Parent = OpenButton
local oGlow = Instance.new("UIStroke")
oGlow.Color = BORDER_BLUE
oGlow.Thickness = 3
oGlow.Parent = OpenButton

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0, 650, 0, 450)
MainFrame.Position = UDim2.new(0.5, -325, 0.5, -225)
MainFrame.BackgroundColor3 = BG_BLACK
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
local mCorner = Instance.new("UICorner")
mCorner.CornerRadius = UDim.new(0, 20)
mCorner.Parent = MainFrame
local mGlow = Instance.new("UIStroke")
mGlow.Color = BORDER_BLUE
mGlow.Thickness = 2
mGlow.Parent = MainFrame

local TitleBar = Instance.new("Frame")
TitleBar.Name = "TitleBar"
TitleBar.Parent = MainFrame
TitleBar.Size = UDim2.new(1, 0, 0, 65)
TitleBar.BackgroundColor3 = DEEP_BLUE
TitleBar.BorderSizePixel = 0
local tCorner = Instance.new("UICorner")
tCorner.CornerRadius = UDim.new(0, 20)
tCorner.Parent = TitleBar

local TitleText = Instance.new("TextLabel")
TitleText.Parent = TitleBar
TitleText.Size = UDim2.new(1, -150, 1, 0)
TitleText.Position = UDim2.new(0, 30, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "N1TE HUB <font color='#0050FF'>V2</font>"
TitleText.RichText = true
TitleText.TextColor3 = TEXT_WHITE
TitleText.TextSize = 26
TitleText.Font = Enum.Font.GothamBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left

local CloseButton = Instance.new("TextButton")
CloseButton.Parent = TitleBar
CloseButton.Size = UDim2.new(0, 45, 0, 45)
CloseButton.Position = UDim2.new(1, -60, 0.5, -22.5)
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 40, 40)
CloseButton.Text = "✕"
CloseButton.TextColor3 = TEXT_WHITE
CloseButton.TextSize = 22
CloseButton.Font = Enum.Font.GothamBold
CloseButton.BorderSizePixel = 0
local cCorner = Instance.new("UICorner")
cCorner.CornerRadius = UDim.new(1, 0)
cCorner.Parent = CloseButton

local Sidebar = Instance.new("Frame")
Sidebar.Parent = MainFrame
Sidebar.Size = UDim2.new(0, 160, 1, -65)
Sidebar.Position = UDim2.new(0, 0, 0, 65)
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Sidebar.BorderSizePixel = 0
local SidebarList = Instance.new("UIListLayout")
SidebarList.Parent = Sidebar
SidebarList.Padding = UDim.new(0, 12)
SidebarList.HorizontalAlignment = Enum.HorizontalAlignment.Center

local ContentArea = Instance.new("Frame")
ContentArea.Parent = MainFrame
ContentArea.Size = UDim2.new(1, -180, 1, -85)
ContentArea.Position = UDim2.new(0, 170, 0, 75)
ContentArea.BackgroundTransparency = 1

local Tabs = {}
local function createTab(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.9, 0, 0, 50)
    btn.BackgroundColor3 = BUTTON_BLACK
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(160, 160, 160)
    btn.TextSize = 16
    btn.Font = Enum.Font.GothamMedium
    btn.Parent = Sidebar
    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(0, 12)
    bCorner.Parent = btn
    
    local container = Instance.new("ScrollingFrame")
    container.Size = UDim2.new(1, 0, 1, 0)
    container.BackgroundTransparency = 1
    container.BorderSizePixel = 0
    container.ScrollBarThickness = 5
    container.Visible = false
    container.Parent = ContentArea
    local list = Instance.new("UIListLayout")
    list.Parent = container
    list.Padding = UDim.new(0, 15)
    list.HorizontalAlignment = Enum.HorizontalAlignment.Center
    
    btn.MouseButton1Click:Connect(function()
        for _, t in pairs(Tabs) do
            t.Btn.BackgroundColor3 = BUTTON_BLACK
            t.Btn.TextColor3 = Color3.fromRGB(160, 160, 160)
            t.Container.Visible = false
        end
        btn.BackgroundColor3 = DEEP_BLUE
        btn.TextColor3 = TEXT_WHITE
        container.Visible = true
    end)
    
    local tab = {Btn = btn, Container = container}
    table.insert(Tabs, tab)
    return container
end

local CombatTab = createTab("COMBAT")
local VisualTab = createTab("VISUALS")
local PlayerTab = createTab("PLAYER")
local MiscTab = createTab("MISC")
local ServerTab = createTab("SERVER")

local function createToggle(parent, text, defaultValue, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 55)
    frame.BackgroundColor3 = BUTTON_BLACK
    frame.Parent = parent
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -90, 1, 0)
    label.Position = UDim2.new(0, 20, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = TEXT_WHITE
    label.TextSize = 16
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0, 60, 0, 30)
    toggle.Position = UDim2.new(1, -75, 0.5, -15)
    toggle.BackgroundColor3 = defaultValue and DEEP_BLUE or Color3.fromRGB(60, 60, 60)
    toggle.Text = ""
    toggle.Parent = frame
    local tCorner = Instance.new("UICorner")
    tCorner.CornerRadius = UDim.new(1, 0)
    tCorner.Parent = toggle
    
    local circle = Instance.new("Frame")
    circle.Size = UDim2.new(0, 26, 0, 26)
    circle.Position = defaultValue and UDim2.new(1, -29, 0.5, -13) or UDim2.new(0, 3, 0.5, -13)
    circle.BackgroundColor3 = TEXT_WHITE
    circle.Parent = toggle
    local cCorner = Instance.new("UICorner")
    cCorner.CornerRadius = UDim.new(1, 0)
    cCorner.Parent = circle
    
    local state = defaultValue
    toggle.MouseButton1Click:Connect(function()
        state = not state
        toggle.BackgroundColor3 = state and DEEP_BLUE or Color3.fromRGB(60, 60, 60)
        local targetPos = state and UDim2.new(1, -29, 0.5, -13) or UDim2.new(0, 3, 0.5, -13)
        TweenService:Create(circle, TweenInfo.new(0.2), {Position = targetPos}):Play()
        callback(state)
    end)
end

local function createSlider(parent, text, min, max, defaultValue, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 85)
    frame.BackgroundColor3 = BUTTON_BLACK
    frame.Parent = parent
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 40)
    label.Position = UDim2.new(0, 20, 0, 5)
    label.BackgroundTransparency = 1
    label.Text = text .. ": " .. defaultValue
    label.TextColor3 = TEXT_WHITE
    label.TextSize = 15
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame
    
    local sliderBack = Instance.new("Frame")
    sliderBack.Size = UDim2.new(1, -40, 0, 10)
    sliderBack.Position = UDim2.new(0, 20, 0, 55)
    sliderBack.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    sliderBack.Parent = frame
    
    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new((defaultValue - min) / (max - min), 0, 1, 0)
    sliderFill.BackgroundColor3 = BORDER_BLUE
    sliderFill.Parent = sliderBack
    
    local sliderBtn = Instance.new("TextButton")
    sliderBtn.Size = UDim2.new(0, 24, 0, 24)
    sliderBtn.Position = UDim2.new((defaultValue - min) / (max - min), -12, 0.5, -12)
    sliderBtn.BackgroundColor3 = TEXT_WHITE
    sliderBtn.Text = ""
    sliderBtn.Parent = sliderBack
    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(1, 0)
    bCorner.Parent = sliderBtn
    
    local dragging = false
    local function update(input)
        local pos = math.clamp((input.Position.X - sliderBack.AbsolutePosition.X) / sliderBack.AbsoluteSize.X, 0, 1)
        local val = math.floor(min + (max - min) * pos)
        sliderFill.Size = UDim2.new(pos, 0, 1, 0)
        sliderBtn.Position = UDim2.new(pos, -12, 0.5, -12)
        label.Text = text .. ": " .. val
        callback(val)
    end
    
    sliderBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then update(input) end
    end)
end


createToggle(CombatTab, "Enable Aimbot", false, function(v) Settings.Aimbot.Enabled = v end)
createToggle(CombatTab, "Wall Check", true, function(v) Settings.Aimbot.WallCheck = v end)
createToggle(CombatTab, "Show FOV Circle", true, function(v) Settings.Aimbot.Visible = v end)
createSlider(CombatTab, "FOV Radius", 10, 800, 150, function(v) Settings.Aimbot.FOV = v end)
createToggle(CombatTab, "Hitbox Expander", false, function(v) Settings.Combat.HitboxEnabled = v end)
createSlider(CombatTab, "Hitbox Size", 1, 20, 2, function(v) Settings.Combat.HitboxSize = v end)
createToggle(CombatTab, "Trigger Bot", false, function(v) Settings.Combat.TriggerBot = v end)


createToggle(VisualTab, "ESP Master", true, function(v) Settings.Visuals.Enabled = v end)
createToggle(VisualTab, "Show Boxes", true, function(v) Settings.Visuals.Boxes = v end)
createToggle(VisualTab, "Show Names", true, function(v) Settings.Visuals.Names = v end)
createToggle(VisualTab, "Show Tracers", true, function(v) Settings.Visuals.Tracers = v end)
createToggle(VisualTab, "Show Health Bar", true, function(v) Settings.Visuals.Health = v end)
createToggle(VisualTab, "Show Distance", true, function(v) Settings.Visuals.Distance = v end)
createToggle(VisualTab, "Skeleton ESP", false, function(v) Settings.Visuals.Skeleton = v end)
createToggle(VisualTab, "Head Dot", false, function(v) Settings.Visuals.HeadDot = v end)


createToggle(PlayerTab, "Manual Fly", false, function(v) Settings.Movement.Fly = v end)
createSlider(PlayerTab, "Fly Speed", 10, 500, 50, function(v) Settings.Movement.FlySpeed = v end)
createSlider(PlayerTab, "Walk Speed", 16, 400, 16, function(v) if LocalPlayer.Character then LocalPlayer.Character.Humanoid.WalkSpeed = v end end)
createToggle(PlayerTab, "Infinite Jump", false, function(v) Settings.Movement.InfiniteJump = v end)
createToggle(PlayerTab, "NoClip", false, function(v) Settings.Movement.NoClip = v end)
createToggle(PlayerTab, "Spin Bot", false, function(v) Settings.Movement.SpinBot = v end)
createToggle(PlayerTab, "Spider Mode", false, function(v) Settings.Movement.Spider = v end)
createToggle(PlayerTab, "Water Walk", false, function(v) Settings.Movement.WaterWalk = v end)

createToggle(MiscTab, "Anti Afk", true, function(v) Settings.Utility.AntiAFK = v end)
createToggle(MiscTab, "No Fog", false, function(v) Settings.Utility.NoFog = v; if v then Lighting.FogEnd = 1e10 else Lighting.FogEnd = 1000 end end)
createToggle(MiscTab, "Full Bright", false, function(v) Settings.Utility.FullBright = v; if v then Lighting.Brightness = 5; Lighting.Ambient = Color3.new(1,1,1) else Lighting.Brightness = 1; Lighting.Ambient = Color3.new(0,0,0) end end)
createToggle(MiscTab, "Chat Spammer", false, function(v) Settings.Utility.ChatSpam = v end)


local RejoinBtn = Instance.new("TextButton")
RejoinBtn.Size = UDim2.new(1, -10, 0, 55)
RejoinBtn.BackgroundColor3 = DEEP_BLUE
RejoinBtn.Text = "Rejoin Server"
RejoinBtn.TextColor3 = TEXT_WHITE
RejoinBtn.Font = Enum.Font.GothamBold
RejoinBtn.Parent = ServerTab
local rjCorner = Instance.new("UICorner")
rjCorner.CornerRadius = UDim.new(0, 12)
rjCorner.Parent = RejoinBtn
RejoinBtn.MouseButton1Click:Connect(function() TeleportService:Teleport(game.PlaceId, LocalPlayer) end)

local ServerHopBtn = Instance.new("TextButton")
ServerHopBtn.Size = UDim2.new(1, -10, 0, 55)
ServerHopBtn.BackgroundColor3 = DEEP_BLUE
ServerHopBtn.Text = "Server Hop"
ServerHopBtn.TextColor3 = TEXT_WHITE
ServerHopBtn.Font = Enum.Font.GothamBold
ServerHopBtn.Parent = ServerTab
local shCorner = Instance.new("UICorner")
shCorner.CornerRadius = UDim.new(0, 12)
shCorner.Parent = ServerHopBtn
ServerHopBtn.MouseButton1Click:Connect(function()
    local Api = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100"
    local _Servers = HttpService:JSONDecode(game:HttpGet(Api))
    local _Server = _Servers.data[math.random(1, #_Servers.data)]
    TeleportService:TeleportToPlaceInstance(game.PlaceId, _Server.id, LocalPlayer)
end)


local function makeDraggable(frame, handle)
    local dragging, dragInput, dragStart, startPos
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
makeDraggable(MainFrame, TitleBar)
makeDraggable(OpenButton, OpenButton)

OpenButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
CloseButton.MouseButton1Click:Connect(function() MainFrame.Visible = false end)


local function isVisible(part)
    if not Settings.Aimbot.WallCheck then return true end
    local ray = Ray.new(Camera.CFrame.Position, (part.Position - Camera.CFrame.Position).Unit * 1000)
    local hit, pos = Workspace:FindPartOnRayWithIgnoreList(ray, {LocalPlayer.Character, Camera})
    if hit and hit:IsDescendantOf(part.Parent) then return true end
    return false
end

RunService.RenderStepped:Connect(function()
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    FOVCircle.Radius = Settings.Aimbot.FOV
    FOVCircle.Visible = Settings.Aimbot.Visible and Settings.Aimbot.Enabled

    if Settings.Aimbot.Enabled then
        local target = nil
        local dist = Settings.Aimbot.FOV
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild(Settings.Aimbot.AimPart) then
                if isVisible(p.Character[Settings.Aimbot.AimPart]) then
                    local pos, onScreen = Camera:WorldToViewportPoint(p.Character[Settings.Aimbot.AimPart].Position)
                    if onScreen then
                        local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                        if d < dist then dist = d; target = p.Character[Settings.Aimbot.AimPart] end
                    end
                end
            end
        end
        if target then Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Position), Settings.Aimbot.Smoothness) end
    end
    
    if Settings.Movement.Fly then
        local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            if not FlyBodyVelocity then
                FlyBodyVelocity = Instance.new("BodyVelocity", root)
                FlyBodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                FlyBodyGyro = Instance.new("BodyGyro", root)
                FlyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
                LocalPlayer.Character.Humanoid.PlatformStand = true
            end
            FlyBodyVelocity.Velocity = Camera.CFrame.LookVector * Settings.Movement.FlySpeed
            FlyBodyGyro.CFrame = Camera.CFrame
        end
    else
        if FlyBodyVelocity then FlyBodyVelocity:Destroy(); FlyBodyVelocity = nil end
        if FlyBodyGyro then FlyBodyGyro:Destroy(); FlyBodyGyro = nil end
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then LocalPlayer.Character.Humanoid.PlatformStand = false end
    end
    
    if Settings.Movement.NoClip and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end

    if Settings.Movement.SpinBot and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(20), 0)
    end

    if Settings.Combat.HitboxEnabled then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                p.Character.HumanoidRootPart.Size = Vector3.new(Settings.Combat.HitboxSize, Settings.Combat.HitboxSize, Settings.Combat.HitboxSize)
                p.Character.HumanoidRootPart.Transparency = 0.7
                p.Character.HumanoidRootPart.CanCollide = false
            end
        end
    end


    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local char = p.Character
            if char and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0 and char:FindFirstChild("HumanoidRootPart") then
                local root = char.HumanoidRootPart
                local pos, onScreen = Camera:WorldToViewportPoint(root.Position)
                
                if onScreen and Settings.Visuals.Enabled then
                    if not ESPCache[p] then
                        ESPCache[p] = { Box = Drawing.new("Square"), Name = Drawing.new("Text"), Tracer = Drawing.new("Line"), HealthBar = Drawing.new("Line"), HealthBarBack = Drawing.new("Line") }
                    end
                    local esp = ESPCache[p]
                    local color = BORDER_BLUE
                    local headPos = Camera:WorldToViewportPoint(char.Head.Position + Vector3.new(0, 0.5, 0))
                    local legPos = Camera:WorldToViewportPoint(root.Position - Vector3.new(0, 3, 0))
                    local h = math.abs(headPos.Y - legPos.Y)
                    local w = h / 1.5
                    
                    esp.Box.Visible = Settings.Visuals.Boxes
                    esp.Box.Size = Vector2.new(w, h)
                    esp.Box.Position = Vector2.new(pos.X - w/2, pos.Y - h/2)
                    esp.Box.Color = color
                    esp.Box.Thickness = 2
                    
                    esp.Name.Visible = Settings.Visuals.Names
                    esp.Name.Text = p.Name .. (Settings.Visuals.Distance and " [" .. math.floor((root.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude) .. "m]" or "")
                    esp.Name.Position = Vector2.new(pos.X, pos.Y - h/2 - 20)
                    esp.Name.Center = true
                    esp.Name.Outline = true
                    esp.Name.Color = TEXT_WHITE
                    
                    esp.Tracer.Visible = Settings.Visuals.Tracers
                    esp.Tracer.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                    esp.Tracer.To = Vector2.new(pos.X, pos.Y + h/2)
                    esp.Tracer.Color = color
                    
                    if Settings.Visuals.Health then
                        local healthPercent = char.Humanoid.Health / char.Humanoid.MaxHealth
                        esp.HealthBarBack.Visible = true
                        esp.HealthBarBack.From = Vector2.new(pos.X - w/2 - 5, pos.Y + h/2)
                        esp.HealthBarBack.To = Vector2.new(pos.X - w/2 - 5, pos.Y - h/2)
                        esp.HealthBarBack.Color = Color3.new(0,0,0)
                        esp.HealthBarBack.Thickness = 4
                        
                        esp.HealthBar.Visible = true
                        esp.HealthBar.From = Vector2.new(pos.X - w/2 - 5, pos.Y + h/2)
                        esp.HealthBar.To = Vector2.new(pos.X - w/2 - 5, pos.Y + h/2 - (h * healthPercent))
                        esp.HealthBar.Color = Color3.new(0,1,0):Lerp(Color3.new(1,0,0), 1 - healthPercent)
                        esp.HealthBar.Thickness = 2
                    else
                        esp.HealthBar.Visible = false
                        esp.HealthBarBack.Visible = false
                    end
                else
                    if ESPCache[p] then for _, d in pairs(ESPCache[p]) do d.Visible = false end end
                end
            else
                if ESPCache[p] then for _, d in pairs(ESPCache[p]) do d.Visible = false end end
            end
        end
    end
end)

LocalPlayer.Idled:Connect(function()
    if Settings.Utility.AntiAFK then
        game:GetService("VirtualUser"):CaptureController()
        game:GetService("VirtualUser"):ClickButton2(Vector2.new())
    end
end)

Tabs[1].Btn.BackgroundColor3 = DEEP_BLUE
Tabs[1].Btn.TextColor3 = TEXT_WHITE
Tabs[1].Container.Visible = true
